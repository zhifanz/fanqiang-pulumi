[PARSER]
    name info
    format regex
    regex INF (?<protocol>[^ ]+) connected lAddr=[^ ]+ rAddr=(?<host>[^:]+):(?<port>\d+) mode=[^ ]+ rule=(?<rule>[^ ]+) proxy=(?<proxy>[^ ]+)
    types protocol:string host:string port:integer rule:string proxy:string
